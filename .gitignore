# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*

# Image-generation pipeline — large local-only artifacts (~2.5GB)
/output/
/public/dishes/
dish-gallery/node_modules/
dish-gallery/dist/
dish-gallery/.vercel/
# Midjourney originals (~250MB of 1024x1024 renders). Only the processed output
# under public/ ships; regenerate it with the scripts/make_*_cards.py scripts.
/Avatares/
# Local-only review sheets, synthetic receipts and approval scratch (not app code)
dish-gallery/review/
approvals-live.json
/Tickets Compra Sintéticos/
# Ledger local de la pasada de enriquecimiento (scripts/enrich-recipe-steps.mjs)
scripts/.enrich-base-done.json

# Salidas de las pruebas de generación de fotos: son cientos de MB de imágenes
# descartables que se regeneran con scripts/gen-test-photos.mjs.
scripts/test-photos/
scripts/prompt-test/

# Caché del CLI de Supabase
supabase/.temp/
